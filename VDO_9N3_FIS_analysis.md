# Polo 9N3 Full-FIS кластер — анализ ROM/EEPROM (черновик под патч)

Кластер: **6Q0920843 KOMBIINSTRUMENT VDO V06**, ПО **VQMJ07HH 08.40**, ядро **65C02**.
Источник протокола/команд: gmenounos/kw1281test. Всё ниже получено из ваших дампов
(read-only анализ), номера адресов проверены по коду.

## 1. Что за файлы вы прислали

- **Пять `VQMJ07HH_bank*.bin`** — это дамп внутреннего масочного ROM микроконтроллера
  кластера (то, что читается командой `DumpNecRom`/`DumpRom`), разбитый на банки.
- **`6Q0920843_<...>_<...>km_<...>.bin` (2048 байт)** — это внешний EEPROM кластера
  (93C86, 2K×8). Здесь лежат кодировка, иммо-данные, счётчики, адаптации.

## 2. Карта памяти (проверена)

| Диапазон CPU | Что | Файл |
|---|---|---|
| `$0000–$1FFF` | RAM + I/O (I/O ~`$1F00`, UART/таймеры `$0D..`, флаги `$08–$09`) | — |
| `$2000–$7FFF` | фиксированный ROM | `bank0` (offset = addr − 0x2000) |
| `$8000–$FFFF` | **страничный** ROM | `bank1..4`, выбор записью в `$1F0A` |

- Вектор сброса `RST=$2089`, `NMI=$638A`, `IRQ=$638F` — они лежат в **bank1**,
  значит bank1 — загрузочная страница. У bank2/3/4 область векторов пустая (`$FF`) —
  это оверлейные/данные-страницы, которые подкачиваются в окно `$8000–$FFFF`.
- Переключение банка — запись в **`$1F0A`** (в коде bank0 встречаются
  `LDA #$11 / STA $1F0A … LDA #$10 / STA $1F0A` вокруг вызова). Это ключевой факт:
  при статическом дизассемблировании `$8000–$FFFF` нужно держать в уме, какой банк
  активен, иначе адреса «поедут».

## 3. Разбор того, что вы уже замеряли ($026C–$0273)

Это **не EEPROM**, а рабочая область RAM. По ней проходит подпрограмма-таймер
`$F868…$F8B2` (bank2). Расшифровка:

- `$026E/$026F` — 16-битный счётчик (LE). У вас: `1B D9` = `0xD91B` = 55579.
- `$0270/$0271` = счётчик − 60 (`$3C`). У вас на ХХ: `DF D8` = `0xD8DF` = 55519 = 55579−60. ✔
- `$0272` — суб-счётчик 0…20 (`$14`), тикает только когда двигатель работает
  (гейт по флагу `$8C`; `$88 bit1` — сброс). Отсюда ваши значения `00→14`.
- `$0273` — регистр статус-битов таймера: `bit4($10)` = «накопили ≥60»,
  `bit5($20)` — тик чётности, `bit7($80)` — служебный.
- `$026C/$026D` (`E2 01`/`D9 01`) — соседний параметр, меняется между зажигание/ХХ;
  трогается той же группой процедур.

Ядро логики (`$F893`):
```
SEC ; LDA $026E ; SBC #$3C ; STA $0270
    ; LDA $026F ; SBC #$00 ; STA $0271
BCC -> TRB #$10 $0273   (сбросить «≥60»)
else -> TSB #$10 $0273  (взвести «≥60»)
```
Вывод: это счётчик наработки/времени, гейтованный по «двигатель работает».
Для ретрофита FIS он вам не нужен как цель патча — но полезен как «якорь»:
он показывает, где живут флаги «двигатель работает» (`$8C`, `$088`) и главный
статус-слово `$095A` / `$0991`, на которые завязано пол-прошивки
(`$095A` — 106 ссылок, `$0991` — 45).

## 4. Unlock-код для доступа к EEPROM (подтверждён)

В kw1281test для строки версии **`VQMJ07HH 08.40`** зашит unlock-код
`34 3F 43 39`. То есть чтение/запись EEPROM этого кластера тулза выполняет так:
custom-разблокировка доп.команд → `9D 34 3F 43 39` → seed/key при необходимости.
Сам код в ROM в открытом виде не лежит (сверка идёт иначе) — это ожидаемо.

## 5. ГЛАВНЫЙ ВЫВОД: ваш EEPROM зашифрован (scrambled)

Тестовые дампы из репозитория (`06032.bin`, `VWZ_02755.bin`, `AUZ_03997.bin`) —
это **старые MKIV-кластеры**, там EEPROM читается «в открытую»: видно ASCII
номера (`V07901CX`, `V08921G`) и иммо-ID (`PC18Y77R001`).

Ваш 9N3 Full-FIS — другое поколение. В его 2 КБ:
- присутствуют все 256 значений байта, энтропия высокая;
- **нет** ни одной ASCII-строки (`6Q0920843`, `VQMJ07HH`, иммо-ID — не находятся
  ни напрямую, ни через простые XOR/ADD/битреверс/nibble-swap с любым ключом);
- нет зеркалирования половин и нет чистой периодичности (проверено
  сдвигами 0x80/0x100/0x200/0x400 и автокорреляцией по периодам 2…256).

Значит преобразование **адресо-зависимое** (байт = f(plain, адрес)), а не простой
однобайтовый ключ. Практический смысл: **нельзя взять «эталонный» открытый дамп
Full-FIS и просто залить его**, и нельзя патчить байты «на глаз». Любой корректный
патч должен считаться через тот же кодек, что зашит в ROM.

## 6. Что делать дальше (план под реальный патч)

Порядок строго такой:

1. **Найти в ROM процедуру чтения/дескремблинга EEPROM.** Кластер по SPI/microwire
   читает 93C86 (11-битный адрес), раскодирует в RAM-теневую копию. Нужно найти
   этот драйвер и восстановить формулу кодирования (это то, что превращает «загадку»
   в редактируемый формат). Кандидаты 11-битных shift-циклов уже локализованы
   (`bank0 $4298`, ряд в `bank1 $95C6/$9766/$A1C0…`) — их надо проверить прицельно.
2. **Снять карту теневой RAM-копии EEPROM** (где какой логический байт лежит после
   дескремблинга) и через xref найти, кто читает кодировку/иммо/адаптации.
3. **Идентифицировать байты кодировки Full-FIS.** У вас есть отличный рычаг: снимите
   два EEPROM — с «не-FIS» и с «FIS» поведением (или до/после включения нужной
   опции через диагностику) и сравните после дескремблинга. Дельта укажет точные
   ячейки.
4. **Считать патч обратно через кодер** и записать `WriteEeprom ADDRESS VALUE`
   (или `LoadEeprom`) утилитой kw1281test.

## 7. Практические заметки по kw1281test для этого кластера

- Адрес блока управления: `17` (кластер).
- Полезные команды: `ReadEeprom`, `DumpEeprom START LEN`, `WriteEeprom ADDR VAL`,
  `LoadEeprom START FILE`, `DumpRom`/`ReadROM` (внутренний ROM), `ActuatorTest`,
  `AdaptationRead/Save`, `SetSoftwareCoding CODING WORKSHOP`.
- Часть «Full FIS» опций может конфигурироваться **кодировкой/адаптацией**, а не
  прямой правкой EEPROM. Сначала стоит исчерпать `SetSoftwareCoding` и
  `AdaptationSave` — это безопаснее и обратимо.

## 8. Обязательные меры безопасности

- **Сделайте эталонные копии** всех дампов до любых записей (несколько read-подряд,
  сверьте md5 — они должны совпасть; расхождение = плохой контакт/питание).
- Пишите только при стабильном питании; сорванная запись 93C86 → «кирпич» кластера.
- Меняйте по одному байту, читайте обратно, проверяйте поведение.

---
### Файлы инструментария
`vdo_toolkit.py` — xref по RAM/IO-адресам, дизассемблер диапазонов, поиск байт-паттернов.
Пример: `python3 vdo_toolkit.py dis 2 0xF868 0xF8B3`,
`python3 vdo_toolkit.py xref 0x095A`.

---

## 9. РАСКРЫТ кодек EEPROM (главный результат)

Низкоуровневый драйвер внешней 93C86 — 3-проводной bit-bang на порту **`$1FAC`**
(чтение `$FC43`, запись `$FC7A`, подача команды/адреса `$FD2B`, CS/тайминги
`$FD55/$FD99/$FDxx`). Менеджер хранилища — bank2 `$CAB0–$CCC1`. На **каждое** слово
при чтении/записи вызывается преобразование **`$CDC0`**, которое и есть «шифрование».

Формула (проверена на вашем дампе, roundtrip и дубли сходятся):
- EEPROM = 1024 слова по 16 бит (little-endian).
- Ключ зависит от индекса слова `i`:
  - обычный: `key_lo = (i & 0xFF) ^ 0xC5`, `key_hi = ((i>>8) & 0x03) ^ 0xC5`
  - окно `i = 0x9C..0xA4` (страница 0): ключ из ROM-таблицы `$538F`:
    `key_lo = ROM[$538F+i] ^ 0x53`, `key_hi = ROM[$538F+i+1] ^ 0x53`
  - `i = 0x116`: без преобразования (спец-случай прошивки)
- `raw = value XOR key` (XOR обратим сам себе).

Параметры хранятся **с избыточностью** (по 2–3 копии в соседних слотах) — это
кольцевой лог с выравниванием износа. Именно поэтому «плоский» XOR раньше не
подходил (ключ разный для каждого адреса), и именно дубли доказали корректность:
сырые слова по смещениям 0 и 2 разные (`38 FF` / `39 FF`), а декодируются в одно
и то же `3A FD`.

Инструмент `vdo_eeprom_codec.py` умеет `decode` (сырой→плейнтекст),
`encode` (обратно), `wordraw i value` (посчитать сырые байты для `WriteEeprom`).
Готовый раскодированный образ — `EEPROM_decoded.bin`.

**Осторожно с записью:** это управляемый прошивкой кольцевой лог с контролем
целостности. Нельзя произвольно перезаписывать «живые» слоты параметров — для
кодировки/адаптаций есть штатные каналы (`SetSoftwareCoding`, `AdaptationSave`).
Кодек нужен, чтобы (а) читать плейнтекст-раскладку и (б) считать сырые байты для
контролируемой записи в известно-свободные области.

## 10. План патча «прогон стрелок» через код в EEPROM

Теперь по вашей цели — внедрить в EEPROM код, который ROM исполнит для прогона
стрелок. Что уже есть и что осталось:

Есть (готово):
- Карта памяти + пейджинг (`$1F0A`; `$1F0F` — контекст/страница в ISR).
- Кодек EEPROM (раздел 9) — можем читать/писать любой байт корректно.
- Драйвер и менеджер EEPROM — знаем, как и куда прошивка ходит за данными.
- Массив бит комплектации/кодировки в RAM `$0412–$0415`
  (декодер bank1 `$A258–$A43D`) — где применяются «фичи».
- Путь актуатор-теста стрелок существует → примитив «двинуть стрелку» достижим;
  это ровно та подпрограмма, которую должен звать наш код прогона.

Осталось (решающее, по порядку):
1. **Подтвердить механизм исполнения кода из EEPROM в ЭТОМ ROM.** Техника, которую
   вы описали, работает только если прошивка где-то (а) копирует блок из EEPROM в
   RAM и делает `JSR`, либо (б) берёт из EEPROM вектор и прыгает по нему. Пока такой
   путь в этом ROM НЕ подтверждён — только хранилище данных с кодеком. Это надо
   проверить прицельно (искать `JSR/JMP` по указателю, взятому из теневой копии
   EEPROM). Если пути нет — «оживить» фичу одним EEPROM-байтом нельзя; тогда вариант
   — модификация прошивки (дописать процедуру прогона и повесить на инициализацию по
   KL15), что и делают те, кто «перешивает» панель.
2. **Найти примитив управления стрелкой** (адрес подпрограммы «поставить стрелку в
   угол X»): проще всего — наблюдать RAM (`ReadRAM`) во время `ActuatorTest` шага
   Tachometer/Speedometer и увидеть, какие ячейки меняются; либо дизассемблировать
   обработчик актуатор-теста.
3. **Написать стаб прогона** (65C02): рамп углов всех стрелок 0→max→0 с задержкой,
   вызывая примитив из п.2. Прототип отлаживать через `WriteRAM`+вызов, НЕ в EEPROM.
4. **Хук**: только после п.1 — закодировать стаб+хук кодеком и записать
   `WriteEeprom` в свободную область; сверить чтением, проверить поведение.

Что ускорит работу кардинально (одно из):
- **Донорский дамп** такой же панели `VQMJ07HH 08.40` с УЖЕ включённым прогоном —
  раскодируем оба, дельта укажет и область, и механизм.
- **Стенд с kw1281test** для живых `ReadRAM/ReadROM/DumpMem/WriteRAM` — тогда п.1–3
  проверяются эмпирически и безопасно, без риска для EEPROM.

### Новые файлы
`vdo_eeprom_codec.py` — кодек EEPROM (decode/encode/wordraw).
`EEPROM_decoded.bin` — ваш EEPROM в раскодированном виде.

---

## 11. Приветствие на FIS — где его нет (2026-08-27)

Приветственное сообщение включено записью `$4DF = 0x6D` в датасете EEPROM
(`EEPROM_MAP.md` §5). Приборка после этого показывает при включении зажигания слово
**`welcome`**. Задача была найти саму строку и понять, можно ли заменить её на свою.

**Найти не удалось**, и это не «плохо искали» — три независимых отрицательных результата:

1. **Не ASCII.** Полный проход по всем пяти банкам ПЗУ и по датасету EEPROM. Текст в
   образах есть, и он находится: `PQ24_7 HL Flash`, `KOMBIINSTRUMENT VDO`,
   `IMMOBILIZER VDO`, `CANTREADEEP` в ПЗУ; VIN машины, серийный номер блока,
   номера деталей `6Q0920843   A0V06` и `6N0909901` в EEPROM. Слова `welcome` среди
   них нет ни в каком регистре.
2. **Не сдвинутый алфавит.** Перебор всех 256 смещений (`'a'` = любой байт) при
   одно- и двухбайтовых символах, по разностной сигнатуре слова, то есть поиск находил бы
   строку при любой линейной кодировке букв. Ноль совпадений.
3. **Не находится по форме слова.** В `welcome` буква `e` стоит первой и последней —
   сильный признак. Поиск семибайтовых последовательностей такой формы даёт 5049
   совпадений в банках, то есть признак статистически пустой.

Отсюда два оставшихся объяснения. Либо приветствие **нарисовано растром** — тогда
символьной строки не существует вовсе. Либо оно лежит в части ПЗУ, которую мы **никогда
не снимали**: у нас 152 КБ (банк0 24 КБ + четыре по 32 КБ = 155648 байт), а адресное
пространство модуля подстановок по документации доходит до `$4FFFF`, то есть до 320 КБ.
Шрифты и графика — ровно то, что живёт в таких банках.

### Сколько места под свою надпись

Ответ не зависит от того, где строка. Модуль подстановок имеет **15 слотов**, шесть уже
заняты (три заводских + три наших под хук свипа), остаётся **девять байт**, и только на
месте — без сдвига и без удлинения. Для посимвольного текста это слово той же длины или
короче; для растра девять байт бесполезны.

Реалистичный путь к произвольной надписи — не подмена байтов, а **процедура в патче,
которая пишет в буфер дисплея во время работы**. Место под код есть: блок 286 байт при
потолке 382, около 96 в запасе, и слоты тогда нужны только на хук. Цена — найти буфер FIS
и точку отрисовки, по объёму это сопоставимо со всей работой по свипу.

Что решило бы вопрос быстро — **полный дамп флеша программатором**, тем же способом,
которым получен `eeprom.bin`. Если недостающие банки существуют, шрифт и строка найдутся
сразу, и станет видно, посимвольная она или растровая.

Решение на 2026-08-27: **ничего не трогаем**, штатное `welcome` остаётся.
